import React, { Component } from 'react';
import "./header.sass";

class Header extends Component {
	render () {
		return (
			<header>
				<a id="logo">Truck list</a>
			</header>
		);
	}
}

export default Header;
